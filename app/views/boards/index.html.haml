%h1 Team and Individual Scores
.row
  - @teams_w_boards.each do |team|
    .col-md-4
      %ul.list-group
        %h3
          = team.name
          %span.badge.badge-primary.badge-pill= team.score
        - team.users.each do |user|
          %li.list-group-item
            - if user.board
              = link_to user.email, board_path(user.board)
              %span.badge.badge-primary.badge-pill= user.score
            - else
              = user.email
