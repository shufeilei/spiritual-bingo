%h1
  - if @board.user == current_user
    Your board
  - else
    = "#{@board.user.username}'s board"
.row
  .col-md-6
    %h3
      Board score:
      = @board.score
  .col-md-6
    %h3
      Your team score:
      = @board.user.team.score
%table.table.table-bordered
  - Board::LAYOUT.each_with_index do |row, index_row|
    %tr
      - row.each_with_index do |cell, index_col|
        %td.text-center{style:'border-width: 5px'}
          %div{ data: { toggle: 'tooltip', placement: 'bottom' },
                title: cell[:description], class: cell[:level] }
            = cell[:name]
            #answer
            - if checked_cell = @cells.where(col: index_col, row: index_row).first
              = checked_cell.response
          %div
            = link_to new_board_cell_path(board_id: @board.id, row: index_row, col: index_col), class: 'btn btn-info btn-sm' do
              %i.fa.fa-check-circle
              Check!

:javascript
  $('[data-toggle="tooltip"]').tooltip()
