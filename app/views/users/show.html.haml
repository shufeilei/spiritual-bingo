%h2 Profile
.info
  %strong Username:
  #{@user.username}
%h3 Email: #{@user.email}
%h3 Team: #{@user.team.name}
.actions
  =link_to 'Edit', edit_user_path(@user), class: 'btn btn-success'
  =link_to 'Scoreboard', boards_path
